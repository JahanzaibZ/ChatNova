name: chatnova
description: A Final Year Flutter Project
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  firebase_auth: ^4.3.0
  firebase_core: ^2.9.0
  flutter:
    sdk: flutter
  google_fonts: ^4.0.3
  intl_phone_field: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/
